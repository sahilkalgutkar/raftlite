# raftlite

A from-scratch implementation of the Raft consensus algorithm in Go, driving a
replicated key-value store.

Full documentation lands with the rest of the implementation — see `make build`
and `make test` to get started.
